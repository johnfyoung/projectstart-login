{
  "name": "projectstart-login",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "begin": "yarn --cwd server && yarn --cwd client",
    "start:server": "yarn --cwd server start",
    "start": "concurrently \"yarn --cwd server start\" \"yarn --cwd client start\""
  },
  "devDependencies": {},
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}